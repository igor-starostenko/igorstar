version: 0.1
frontend:
  phases:
    preBuild:
      commands:
        - nvm install 17
    build:
      commands:
        - yarn install
        - yarn eslint src
        - yarn build
  artifacts:
    baseDirectory: public
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
